# Optional — only needed for enhanced mode (richer intel data)
# Phantom works out of the box without any keys (zero-key mode).

VIRUSTOTAL_API_KEY=
ABUSEIPDB_API_KEY=
SHODAN_API_KEY=
URLSCAN_API_KEY=
